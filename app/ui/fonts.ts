import { Inter, Lusitana, Mukta, Eczar } from 'next/font/google';

 
export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({ subsets: ['latin'], weight: ['400', '700'], });

export const mukta = Mukta({ subsets: ['latin'], weight: ['200', '400', '700'], });

export const eczar = Eczar({ subsets: ['latin'], weight: ['400', '600', '700'], });